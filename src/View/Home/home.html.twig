{% extends 'layout.html.twig' %}

{% block content %}
    <div class="container">
        <div class="row">
            {% for row in array %}
                <div><a href=""><img src="{{ }}" alt="{{ }}"></a></div>
            {% endfor %}
        </div>
    </div>      
{% endblock %}
