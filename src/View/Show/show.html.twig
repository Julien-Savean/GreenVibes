{% extends 'layout.html.twig' %}

{% block content %}
<pre>
    {{ dump(request) }}
</pre>
    <div class="container justify-content-center">
        <div class="row">
            <div class="col">
                <div class="plant"><img src="{{ plant.picture }}" alt="{{ plant.name }}"></div>
            </div>
        </div>
    </div>      
{% endblock %}